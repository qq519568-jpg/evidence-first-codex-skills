interface:
  display_name: "双部门电影预演管线"
  short_description: "商业探索优先的导演、Blender与视频AI轻量管线"
  default_prompt: "Use $duo-dept-pipeline to maximize visible content or reusable capability per iteration, choosing human, AI, tools, or paid services by ROI."

policy:
  allow_implicit_invocation: true
