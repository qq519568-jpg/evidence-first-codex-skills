# Third-party notices

Runtime dependencies are pinned but not committed as dependency caches:

- `lunar_python` 1.4.8 — MIT — <https://github.com/6tail/lunar-python>. A copy of its license is retained at `licenses/lunar-python-MIT.txt`.
- `iztro` 2.5.8 — MIT — <https://github.com/SylarLong/iztro>. A copy of its license is retained at `licenses/iztro-MIT.txt`.

Run `scripts/setup_dependencies.ps1` to install them locally into ignored directories. Their transitive dependencies and package metadata are governed by their respective licenses.
